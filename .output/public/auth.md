# Authentication for Pixora

Pixora is a purely client-side browser application. 
Currently, there are no protected server-side APIs that require authentication.

If we introduce API authentication in the future, this file will contain the Agent Registration instructions.
